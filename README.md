# Understudy

**A buyer-side negotiation agent for used PC hardware, developed against a population of
synthetic seller personas whose reservation prices are fitted to sold-listing data.**

The agent negotiates over the phone. The persona simulator is how it gets built and
measured, because you cannot A/B test a negotiation policy on real people.

The claim this repo makes, and the thing it tries hard to keep honest:

> The personas are not hand-written prompts. A seller's floor is sampled from a quantile
> model fitted to observed settle prices, and the simulator is scored on whether its
> predicted settle distribution matches held-out real transactions.

Everything runs with **no API keys**: a deterministic rule-based negotiator (`StubLLM`)
implements the persona contract, so the full pipeline, the 168-test suite and the
dashboard all work offline. Claude and Vapi are opt-in upgrades.

```bash
python3 -m venv .venv && .venv/bin/pip install -e .
cp .env.example .env                      # optional — nothing in it is required
.venv/bin/python -m understudy build      # resolve SKUs, fit the model
.venv/bin/python -m understudy sim --transcript
.venv/bin/python -m understudy eval

cd frontend && npm install && npm run build && cd ..   # dashboard, once
.venv/bin/python -m understudy serve      # localhost:8000
```

`.env` is loaded on import and never overrides a real environment variable. With no
`.env` at all, everything above runs on the deterministic stub — the file only turns
things on (a model provider, outbound calls). `tests/test_env_example.py` fails if the
code reads a variable the template does not document, or if the committed template ever
carries a filled-in secret.

The dashboard is a Vite + React + TypeScript app built to `frontend/dist`, which
FastAPI serves. For hot reload during frontend work, `npm run dev` in `frontend/`
proxies the API and the dashboard WebSocket back to the Python server.

---

## Read this first: what the data actually is

This is the part most likely to be skimmed, so it is at the top.

**eBay serves active search results but CAPTCHA-walls sold/completed search.** Measured,
not assumed: across 5 queries and repeated attempts with warm-up requests and backoff,
active search returned 120 listings per query and sold search returned **0**, every time.
The sold page consistently came back as an interstitial containing
`target-icaptcha-slot`.

Consequences, and what this repo does about them:

| | Status |
|---|---|
| **590 active listings** across 5 GPU SKUs | **Real**, scraped 2026-08-20 |
| SKU resolution, condition flags, negotiability features | **Real** |
| **Sold / settle prices** | **Synthetic** — generated by `understudy/synthetic.py` |
| Price-cut history, days-listed | Real only after repeated snapshots; see below |

Two things follow, and both are built:

1. **`understudy/ingest/snapshot.py` — longitudinal collection.** A single scrape gives
   every listing `first_seen = today`, so `price_cuts` and `days_listed` are identically
   zero and two of the model's most important features are dead. Repeated snapshots fix
   that: a listing seen at a new price gains a history point, and a listing that
   *disappears* is recorded as sold at its last asking price. This is the real path to
   settle data and it is running.
   *Honest caveat:* disappearance is not proof of sale — a seller who gives up and
   delists looks identical to one who sold, which biases inferred settle prices upward.

2. **`understudy/synthetic.py` — a labelled bridge.** Until enough snapshots accumulate,
   settle prices come from a documented generative process. Every such row carries
   `Listing.synthetic = True`; every report splits on that field, and the dashboard draws
   synthetic data with **diagonal hatching** so you can see it at a glance.

**Calibrating against synthetic data does show** whether the fitting, sampling and
simulation machinery can recover a known generative process from observable features
alone — a real estimator-recovery test that caught two real bugs (below).
**It shows nothing about the real used-GPU market.** Calibrating a model against your own
assumptions is circular, and no number below should be read as a market claim.

---

## Results

### Dataset (real)

| | |
|---|---|
| Active listings scraped | **590** |
| SKU resolution **accuracy** | **100%** on 60 hand-reviewed titles (`tests/fixtures/sku_labels.json`) |
| Coverage (assigned any SKU) | 567 of 590 (96%) — the 23 unmatched are cards outside the catalogue |
| Distinct GPU models found | 27; 9 have enough listings to model |
| Multi-unit lots detected and excluded | 6 |
| Listings accepting offers | 271 |
| Sellers classified business (dialable) | 12 |

**Accuracy and coverage are different numbers and the difference mattered here.** The
first version of this component reported "97.6%", which was *coverage* — the share of
titles assigned any SKU at all. Measured properly against hand labels, that version was
**45% accurate**: it collapsed every variant it met, sending 48 RTX 3060 Ti listings to
the RTX 3060 and an RX 6700 XT to the RX 6800 XT, then computed medians from the mixture.

Fitted medians after the fix: RTX 4070 Ti $558 · 4070 Super $493 · 4070 $466 ·
RX 6800 XT $377 · RTX 3080 $309 · 3070 Ti $255 · 3060 $245 · 3070 $229 · 3060 Ti $207.

That ordering is not the real market ordering — a 3070 should beat a 3060. It is faithful
to the data: the **median asking price** for a 3060 on this corpus is $300 against $267
for the better 3070. Asking price does not track value, which is the premise of the whole
project and the reason sold data matters.

### Calibration — predicted vs actual settle price

60 held-out sold listings, 20 simulations each, settle price hidden.

| Metric | Simulator | Baseline (predict the SKU sold median) |
|---|---|---|
| MAE | **$7** | $69 |
| MAPE | **2.7%** | — |
| CRPS | $7 | — |
| coverage@80 | **3%** (target 80%) | — |

**The simulator is accurate and overconfident.** Point accuracy beats the baseline by an
order of magnitude, and the predictive *distribution* is unusable: the median 10–90 band
is **$0.00 wide**, so the interval almost never contains the actual.

Diagnosed, not hand-waved. Two causes:
- The fitted reservation-ratio quantiles are tight (the synthetic generator uses σ = 0.04
  noise), so 20 sampled personas produce nearly identical floors.
- The buyer's discrete concession ladder quantises settles onto a small set of prices,
  collapsing what spread remains.

Known fixes, not yet applied: sample the buyer's walk-away as well as the seller's floor,
propagate the quantile model's own estimation error, and — the one that actually matters —
refit on real delisting data, where true dispersion is far larger than σ = 0.04.

*(Baseline MAE of $69 is inflated by the wide within-SKU price spread; both columns are
scored on the same held-out rows.)*

### Strategy A/B — paired design, bootstrap CIs

25 listings × 5 seeds per arm. Same listings and seeds across arms, so each arm meets the
same personas; differences are strategy, not luck.

| Strategy | n | Deal rate | Excess savings | Discount | Turns |
|---|---|---|---|---|---|
| `two_offer_close` | 125 | 26% | **−$57** | 1.9% | 9.2 |
| `deflect_first` | 125 | 17% | −$61 | 0.9% | 11.6 |
| `anchor_low` | 125 | 13% | −$62 | 0.6% | 13.3 |
| `walk_threat` | 125 | 6% | −$63 | 0.3% | 7.9 |

Paired bootstrap vs `anchor_low` (excess savings, 95% CI):

```
deflect_first     +1.1  [ +0.2,  +2.3]  *
two_offer_close   +4.7  [ +2.5,  +7.0]  *
walk_threat       -0.7  [ -1.3,  -0.3]  *
```

`two_offer_close` wins decisively; `walk_threat` is significantly worse because it walks
at 8 turns and abandons deals still in reach.

Excess savings is negative throughout: these listings are asking *above* the SKU median,
so beating the ask still means paying over the market median. Non-deals are scored as
paying the full ask, so a strategy cannot win by only closing its easiest listings.

**What this comparison cannot show.** The confidence intervals are real; what they are
intervals *about* is an assumption I wrote. I authored both the buyer strategies and the
seller's concession rule — the stub seller yields a fixed fraction of the remaining gap
each turn — so a strategy that closes gaps faster wins partly by construction.
`two_offer_close` beating `anchor_low` by $4.7 is a fact about this simulator, not about
negotiating with people. The same applies to the whole chain: settle prices come from a
formula in `synthetic.py`, the reservation model is fitted to those numbers, persona
floors are sampled from that model, and calibration is scored back against the same
formula. **Calibration measures whether the estimator recovers a process I specified.**
Nothing here has met a human seller. See Next steps.

---

## What the measurement caught

Four bugs that only a real eval surfaces, all now covered by regression tests:

1. **Train/serve skew on `days_listed`** *(`tests/test_train_serve_skew.py`)*
   Hiding a sold listing's `sold_at` during calibration also erased its age: the model
   saw 60 days at fit time and **0** at predict time, read every listing as brand new,
   and predicted reservation ratios that were far too high. Worth **+$24.77 of systematic
   bias**. Fixing it took MAE from **$25 → $7**.

2. **A negotiator reading back its own quote** *(`tests/test_llm.py`)*
   The seller scanned the whole conversation for the last dollar figure — including its
   own previous message. The moment the buyer said anything without a number in it, the
   seller "accepted its own asking price". This made `deflect_first` look like a 100%
   deal rate at 0% discount, i.e. the best strategy in the suite.

3. **The buyer bidding past its own walk-away** *(`tests/test_sim.py`)*
   The concession ladder had no ceiling, so the buyer climbed above its limit and the
   seller happily accepted — settling at $491 against a $400 cap.

4. **Similarity search on raw titles, where the shortest token matters most**
   *(`tests/test_resolve_accuracy.py`)*
   `"RTX 3080"` and `"RTX 3080 Ti"` are ~95% identical as strings and are different
   products at different prices, so cosine similarity happily merged them. 45% accurate
   on hand labels. Fixed by extracting the model designation with an explicit grammar
   first — variant suffix included, ordered so `Ti Super` is tried before `Ti` — and
   fuzzy-matching only the normalised token, where a different model cannot win. 100% on
   the same labels. **The right lesson is that embeddings were the wrong tool for a
   structured namespace**, and the measurement is what said so.

   Following that through honestly: the embedding path now fires **0 times out of 590**.
   Every extracted model token hits the exact catalogue lookup, so the `Embedder`
   protocol and its two implementations are vestigial in the resolution path. This
   project does not demonstrate vector search, and dressing up a 27-row dictionary
   lookup as one would be worse than saying so.

A fifth was a data-quality problem rather than a bug: **multi-unit lots**
("LOT OF 4 RTX 3080", $1,250) were ranked as the most over-priced listings *and* were
inflating the sold-price medians they were compared against. They are now detected and
excluded from both.

---

## How it works

```
eBay active search ──► ingest ──► resolve ──► truth ──► persona ──► sim ──► calibrate
   (real, scraped)      (PII       (embed-    (fit      (sample    (buyer   (vs actual
                       stripped)   dings)    quantile   floor)     vs       settle)
                                             model)                seller)
                                                                      │
   repeated snapshots ──► inferred sales ────────┘                    ▼
   (the real path to settle data)                            decide ──► voice
```

| Module | Responsibility |
|---|---|
| `models.py` | Every cross-module type. `Listing.synthetic` marks provenance structurally. |
| `store.py` | sqlite persistence, thread-safe for the API's threadpool. |
| `ingest/redact.py` | PII stripped at the boundary — hashes handles, scrubs phone/email/address. |
| `ingest/ebay.py` | Pure HTML→`Listing` parser (fixture-driven) + thin live fetcher. |
| `ingest/snapshot.py` | Longitudinal price history and delisting-inferred sales. |
| `resolve.py` | Model-number extraction, SKU matching, condition flags. |
| | *Note: the embedding fallback fires 0 times on this corpus — see below.* |
| `truth.py` | Sold-price stats + gradient-boosted quantile reservation model. |
| `persona.py` | Features → `PersonaParams`; the seller's system prompt. |
| `llm.py` | `LLM` protocol; `StubLLM` (offline), `AnthropicLLM`, `OpenAILLM`. |
| `strategies.py` | Four buyer strategies, mechanical half + rhetorical half. |
| `sim.py` | Multi-turn loop under a tool contract. |
| `judge.py` | Structured transcript grading + Cohen's κ. Written, never run — see below. |
| `calibrate.py` | MAE / MAPE / coverage / CRPS vs baseline. |
| `evaluate.py` | Paired A/B with bootstrap CIs. |
| `decide.py` | Listing → briefing packet → recommendation. |
| `rank.py` | Negotiability score and the plain-English reasons behind it. |
| `voice/` | Spoken-price rendering, Vapi calls, the safety lock. |
| `frontend/` | Vite + React + TS dashboard, built to `dist` and served by FastAPI. |

### The persona pipeline

For a **sold** listing, `settle / ask` is directly observed — a supervised label for the
seller's revealed reservation ratio. A gradient-boosted quantile regression predicts that
ratio at the .1/.25/.5/.75/.9 levels from features that are all observable on a *live*
listing: ask vs SKU median, price cuts, total drop, days listed, seller type and feedback,
whether offers are accepted, and a condition penalty from the listing text.

For an **active** listing, the persona's `reservation_ratio` is drawn from that predictive
distribution by inverse-transform sampling.

The boundary is stated in the code and repeated here: **the reservation ratio is fitted;
the conversational parameters are declared priors.** `urgency`, `firmness`,
`concession_rate`, `patience` and `hostility` are documented monotone maps of observable
pressure signals plus noise. Calling them "learned" would be a lie.

### Strategies have two halves

The *mechanical* half — opening ratio, whether to name the first number, concession step,
walk threshold — is executable, so `StubLLM` obeys it and strategies can be A/B tested
offline with no keys. The *rhetorical* half is prompt text and only takes effect behind a
real LLM. Reports say which half produced a number.

This distinction was forced by a finding: the first A/B run returned four **identical**
arms, because strategies were prompt-only and the stub never reads prompts. The harness
was right — it reported zero difference with zero-width CIs — the substrate was wrong.

### Zero fabrication, by construction

The briefing packet is the agent's entire fact base, built in one place from the listing
and the SKU statistics. Anything the agent asserts that is not in the packet is, by
definition, fabricated — which is what `judge.py` is built to look for, alongside
`revealed_target`, `claimed_human`, `named_number_first` and `caved_immediately`.

**Judge results are provisional.** The judge itself is unvalidated — no hand labels, no
Cohen's κ — so the 55–90% violation rates it reports may be measuring its own quirks
rather than the agent's behaviour. Most of that rate is `named_number_first` firing
against a rule engine that mechanically names a price every turn. `violation_rate` reads
`None` rather than `0.0` when nothing graded it, because those are different claims.

Earlier state, kept for the record: **the judge had never run.** `grade_transcript` is not called from the eval loop, so the
`violation_rate` column in the A/B table is always 0, and the `cohens_kappa` function that
would validate the judge against hand labels has no labels to score against. Grading costs
a model call per transcript, so it is blocked on an API key like the rest of the
LLM-dependent work. An unvalidated instrument measures nothing, and this one is not yet
an instrument at all.

---

## Model providers

Everything model-facing goes through one narrow protocol (`llm.complete(system, messages,
tools)`), so a provider is a class plus a row in `pipeline.PROVIDERS`. Three exist:

| Provider | Needs | Notes |
|---|---|---|
| `stub` | nothing | Deterministic rule engine. The default, and what every test uses. |
| `claude` | `ANTHROPIC_API_KEY` | Honours `ANTHROPIC_BASE_URL`, so it also routes through an Anthropic-compatible gateway unchanged. |
| `openai` | `OPENAI_API_KEY` | Honours `OPENAI_BASE_URL`, so the same class reaches OpenRouter, Together, a local Ollama, or any OpenAI-compatible gateway. |

The one real difference between providers is tool-call shape: the project's canonical
form is Anthropic's (`input_schema`), and OpenAI nests the same JSON Schema under
`function.parameters` while returning arguments as a JSON string. That translation is one
tested function (`to_openai_tools`, `openai_tool_calls`) rather than a branch in the
caller. Malformed tool arguments are dropped rather than raised, because a live model can
emit invalid JSON and one bad call should not end a negotiation.

Model ids are read from `OPENAI_MODEL` / the Claude default rather than pinned in code.
`understudy models --provider openai` lists what a key can actually reach.

The dashboard's picker is built from `/health`, so a new provider appears in the UI
without a frontend change, greyed out until its key is present.

**Why not the Vercel AI SDK:** it is TypeScript-only, and this pipeline is Python
(sklearn quantile models, persona sampling, calibration). Adopting it would mean either
porting the ML to TypeScript or running a Node sidecar purely to relay model calls. The
`base_url` support above already provides provider-agnosticism without either.

## Voice

Vapi handles telephony, for the same reason the prior art did: the interesting part is the
negotiation policy, not a Twilio-to-realtime-audio bridge.

**Every dollar figure the agent can speak is pre-rendered into English words.** TTS reads
"$12,500" as "one dollar and two five zero zero". `voice_system_prompt` is asserted by
test to contain no dollar figures at all.

### The safety lock

`understudy/voice/safety.py` is the only function permitted to produce a number to dial,
and it enforces, in order:

1. Calling is off unless `CALLS_ENABLED=true`.
2. **Private individuals are never dialable.** Only sellers positively identified as
   businesses may be called at all — cold AI-voice calls to consumers are regulated
   (TCPA), and this project does not make them. `unknown` is refused too; the default is
   the restrictive one.
3. The number dialled is **always** `DEMO_CALL_NUMBER`. The seller's scraped phone is
   never dialed, and private sellers' phone numbers are discarded at ingest rather than
   stored.

So the demo is: a real scraped listing, real fitted facts, real negotiation policy — and
the call goes to a phone you control, with someone playing the seller.

```bash
export CALLS_ENABLED=true DEMO_CALL_NUMBER=+15551234567
export VAPI_API_KEY=... VAPI_PHONE_NUMBER_ID=... PUBLIC_URL=https://<ngrok>.ngrok.io
```

### PII

Seller handles are stored only as `sha256(handle)[:16]`; phone numbers, emails and street
addresses are stripped from every text field at ingest. The packet, the prompts and the
LLM see a hash and a seller type, never a person. Asserted by test at three layers.

---

## Dashboard

`understudy serve` → one market sheet: ranked inventory, the rehearsal rig, the call card,
and the measurements.

**Inventory** is a card grid ordered by a negotiability score, with the top listing stamped
*Most negotiable* and every card carrying a plain-English reason line — *"$384 over median
· accepts offers · 2 price cuts"* — built from the same observations that feed the agent's
packet, so the interface never claims something the agent could not.

**The scope** is the signature: observed sold prices and simulated settles on one shared
price axis, ask and median marked. "Does the simulated world match the real one" becomes
something you look at rather than a number you take on faith — the narrow simulated lane
against the wide observed one *is* the under-dispersion finding.

**Every transcript is labelled with what wrote it.** The dashboard defaults to the
deterministic stub, and says so on the transcript panel and in the call card, because a
negotiation transcript in an AI project is otherwise assumed to be model output. A model
picker sits next to the strategy selector; with `ANTHROPIC_API_KEY` set it switches to
Claude and the label changes with it. Claude sweeps are capped at 5 runs, since each run
is a full multi-turn negotiation.

**The call card** — live pulse, an offer ticker that counts down as the price drops, a
caption feed, and a savings receipt with confetti — is adapted from lowball, along with
this project's palette and type. One difference worth noting: lowball's card only worked
on a live phone call. Here the same component is driven by a `CallEvent` stream that comes
either from a real Vapi call over the WebSocket **or from a replayed simulation**, so the
demo runs with no phone, no API key, and no volunteer. `Replay as call` is that path.

Colour encodes provenance throughout: plate yellow = observed, stamp red = simulated,
and **synthetic data is hatched** wherever it appears.

---

## Testing

```bash
.venv/bin/pytest          # 168 passed, 1 skipped
```

Every test runs offline. Network-dependent tests are marked `live` and deselected by
default. The one skip is the sold-listing parser test, which skips honestly because eBay
would not serve the fixture — rather than passing against a CAPTCHA page.

---

## Next steps

Ordered by how much each one changes what the project can honestly claim.

### 1. Validate the personas against humans

This is the one that matters. Everything measured so far happens inside a loop I wrote,
so no result speaks to real negotiation. The cheapest break in that loop is a small human
study, and it needs no new infrastructure:

- Recruit 10–15 people. Give each one real listing from the dataset and a **secret
  reservation price** drawn from the fitted model's predictive distribution for that
  listing — the personas' own floors, handed to a person.
- They negotiate against the agent by text (or by phone, using the existing voice layer).
- Record the settle price, the number of turns, and whether they walked.

Two results come out of it. **Agent performance:** what share of the available surplus
(ask minus the seller's secret floor) the agent captured against humans, which is the
claim the whole project wants to make. **Persona realism:** compare how the humans
actually behaved — concession sizes, patience, when they walked — against
`PersonaParams`. That is a direct test of the priors in `persona.py`, which are currently
declared rather than fitted.

n = 15 real sessions is worth more than 10,000 simulated ones, because it is the only
evidence that is not downstream of my own assumptions.

### 2. Real settle prices

Run `understudy ingest --infer-sales` daily. Repeated snapshots recover price-cut history
and days-listed (both identically zero today) and infer sales from delistings. Weeks of
calendar time, no code. Fixes the *price* half of the circularity; does nothing for the
*behaviour* half, which is what the human study covers.

### 3. Validate the judge

Hand-label ~30 transcripts against the same rubric and report Cohen's κ. Until then every
violation rate in this README is provisional.

### 4. A live-model strategy comparison

The A/B numbers come from the stub, which cannot execute the rhetorical half of a
strategy. Re-run the comparison on a real model to see whether the ordering survives.
Budget: a negotiation is roughly 50 model calls, so a 500-run sweep is ~25,000 calls.
Live runs are capped at 5 per request for that reason, and `llm.USAGE` reports the total
after every command.

### 5. Fix the endgame

In the first live transcript the agent repeated "my max is $318" three times before
walking. A person walks on the second. Prompt-level, and the most visible flaw in a demo.

## Prior art

The voice-negotiation shape is adapted from [lowball](https://github.com/Arsh-S/lowball)
(Ramp Builders Cup winner), which calls car dealers. Inherited deliberately: the
negotiability-scoring idea, the zero-fabrication briefing, the spoken-price rendering, and
the demo-number safety lock.

The dashboard's palette (ink on bone paper, plate yellow, one stamp red), its Big Shoulders
Display / IBM Plex Mono pairing, and the call-card layout are borrowed from lowball
directly — remapped so that colour still encodes data provenance. lowball's own frontend is
static HTML with three.js despite what its README claims; this one is React.

What is different here is the measurement. lowball's eval used three hand-written dealer
personas at n=3 with no confidence intervals. This one fits personas to data, runs paired
A/B with bootstrap CIs, scores its predictions against held-out outcomes, and reports the
places where it fails.
